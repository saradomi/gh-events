name: "workflow commands"

on: [workflow_dispatch, push ]

jobs:
  context:
    runs-on: ubuntu-latest
    steps:
      - name: Use some context
        run: |
          echo "Hello! $MY_ACTION"
          echo "Hello $MY_ACTOR"
        env:
          MY_ACTION: ${{ github.action }}
          MY_ACTOR: ${{ github.actor }}
  default-env-variables:
    runs-on: ubuntu-latest
    steps:
      - name: Display some default variables
        run: |
          echo "Github action: $GITHUB_ACTION_REPOSITORY"
          echo "Gihub repository owner: $GITHUB_REPOSITORY_OWNER"
          echo "Debug: $RUNNER_NAME $RUNNER_DEBUG"
  try-custom-action:
    runs-on: ubuntu-latest
    steps:
      - name: Try custom action
        id: Hello
        uses: saradomi/experimental-action@0.0.3
        with: 
          name: Nyx
      - name: Get the output
        run: echo "${{steps.Hello.outputs.greeting}}"